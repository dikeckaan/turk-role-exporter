{
  "name": "turk-role-exporter",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "lint": "eslint docs/js/ worker/",
    "format": "prettier --write docs/js/ worker/ docs/css/",
    "test": "node --test tests/*.test.js",
    "update-fallback": "node scripts/update-fallback.js",
    "build-hakkinda": "node scripts/build-hakkinda.js",
    "update-airband": "node scripts/update-airband.js"
  },
  "dependencies": {
    "@cloudflare/kv-asset-handler": "^0.3.4"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "marked": "^18.0.0",
    "prettier": "^3.0.0"
  }
}
